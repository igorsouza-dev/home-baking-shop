import styled from 'styled-components';

export const ButtonWrapper = styled.button`
  width: 100%;
  background: var(--accent);
  color: var(--primary);
  font-weight: 700;
  border: 0;
  padding: 20px 25px;
  &:hover {
    background: #434112;
  }
`;
